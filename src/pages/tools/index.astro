---
import MarketingLayout from '@/layouts/MarketingLayout.astro'; 

const pageTitle = "Kostenlose Kfz-Händler Tools & Rechner | Autaxo";
const pageDescription = "Praktische Online-Tools für den Gebrauchtwagenhandel: § 25a Quick-Check, Rechnungstexte-Generator und mehr. Kostenlos und ohne Anmeldung.";

const tools = [
  {
    title: "§ 25a Quick-Check",
    description: "Unsicher, ob Differenzbesteuerung erlaubt ist? Dieser interaktive Check führt dich in 3 Klicks zur korrekten Entscheidung.",
    icon: "⚖️", 
    link: "/tools/differenzbesteuerung-check",
    badge: "Essentiell",
    category: "Steuern"
  },
  {
    title: "Rechnungstexte Generator",
    description: "Erstelle rechtssichere Hinweistexte für deine Rechnungen (z.B. für Export, § 25a oder steuerfreie igL).",
    icon: "📝",
    link: "/tools/rechnungstexte-generator",
    badge: "Neu",
    category: "Recht"
  },
  {
    title: "Margen-Rechner (Coming Soon)",
    description: "Berechne deine echte Netto-Marge nach Abzug der Umsatzsteuer auf die Differenz.",
    icon: "🧮",
    link: "/tools/margen-rechner",
    badge: "Neu",
    category: "Kalkulation"
  }
];
---

<MarketingLayout title={pageTitle} description={pageDescription}>
  
  <section class="relative bg-slate-950 text-white py-24 overflow-hidden border-b border-white/5">
    
    <div class="absolute top-0 left-0 w-full h-full opacity-20 pointer-events-none">
      <div class="absolute right-0 top-0 w-96 h-96 bg-blue-600 rounded-full blur-[100px] transform translate-x-1/2 -translate-y-1/2"></div>
      <div class="absolute left-0 bottom-0 w-64 h-64 bg-emerald-600 rounded-full blur-[100px] transform -translate-x-1/2 translate-y-1/2"></div>
    </div>

    <div class="container mx-auto px-4 max-w-5xl relative z-10 text-center">
      <span class="inline-block py-1 px-3 rounded-full bg-slate-800/50 border border-slate-700 text-blue-300 text-sm font-semibold mb-6 backdrop-blur-sm">
        Kostenlose Händler-Tools
      </span>
      <h1 class="text-4xl md:text-6xl font-extrabold mb-6 tracking-tight leading-tight text-white">
        Weniger Kopfzerbrechen.<br/>
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-emerald-400">Mehr Rechtssicherheit.</span>
      </h1>
      <p class="text-lg text-slate-400 max-w-2xl mx-auto mb-10 leading-relaxed">
        Kleine Helfer für die täglichen Fragen im Autohandel. 
        Sofort nutzbar, keine Anmeldung nötig, 100% praxisorientiert.
      </p>
    </div>
  </section>

  <section class="bg-slate-950 py-20 min-h-[50vh]">
    <div class="container mx-auto px-4 max-w-6xl">
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {tools.map((tool) => (
          <a href={tool.link} class="group relative flex flex-col h-full bg-slate-900/50 rounded-2xl shadow-sm border border-white/10 hover:border-blue-500/50 transition-all duration-300 hover:-translate-y-1 overflow-hidden backdrop-blur-sm">
            
            <div class="p-8 flex flex-col h-full">
              {/* Header */}
              <div class="flex justify-between items-start mb-6">
                <div class="w-14 h-14 bg-slate-800 text-3xl flex items-center justify-center rounded-xl group-hover:bg-blue-600 group-hover:text-white transition-colors duration-300 border border-white/5">
                  {tool.icon}
                </div>
                {tool.badge && (
                  <span class="bg-emerald-900/30 text-emerald-400 text-xs font-bold px-3 py-1 rounded-full border border-emerald-500/30">
                    {tool.badge}
                  </span>
                )}
              </div>

              {/* Content - Text White */}
              <h3 class="text-2xl font-bold text-white mb-3 group-hover:text-blue-400 transition-colors">
                {tool.title}
              </h3>
              <p class="text-slate-400 mb-6 leading-relaxed flex-grow">
                {tool.description}
              </p>

              {/* Footer / CTA */}
              <div class="mt-auto pt-6 border-t border-white/10 flex justify-between items-center">
                <span class="text-slate-500 text-sm font-medium">{tool.category}</span>
                <span class="text-blue-400 font-semibold flex items-center gap-2 group-hover:translate-x-1 transition-transform">
                  Tool öffnen 
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                  </svg>
                </span>
              </div>
            </div>
          </a>
        ))}
      </div>

    </div>
  </section>

  <section class="bg-slate-950 py-16 border-t border-white/10">
    <div class="container mx-auto px-4 max-w-4xl text-center">
      <h2 class="text-2xl font-bold text-white mb-4">Warum diese Tools?</h2>
      <p class="text-slate-400 mb-8 leading-relaxed">
        Im Gebrauchtwagenhandel entscheidet oft ein kleines Detail über die Marge oder das Risiko einer Nachzahlung. 
        Unsere Tools basieren auf den Logiken unserer <a href="/blog" class="text-blue-400 underline decoration-blue-500/30 hover:decoration-blue-400 transition-all">Wissensdatenbank</a> und helfen dir, 
        technische Fehler bei § 25a, Exporten und Rechnungsstellung zu vermeiden.
      </p>
    </div>
  </section>

</MarketingLayout>